# 阶段二：建筑结构与第一人称可玩原型

技术栈：**Vite + Three.js + 原生 JavaScript**（未使用 React —— 本项目只有 4 个按钮和 1 个全屏 Overlay，无组件树可言，引入 React 只会增加体积与状态同步成本）。

---

## 1. 核心做法：一份数据，两种用途

```
src/data/building.js         房间矩形 / 门 / 窗 / 楼梯 / 实体构件（阶段一产物）
        ↓
src/geom/build.js（纯 JS）    walls() / slabs() / stairSteps() / colliders() / supports()
        ↙                                          ↘
src/scene/house.js（渲染）                     src/player.js（碰撞）
                                              tools/navcheck.mjs（可达性测试）
                                              tools/walktest.mjs（行走测试）
```

**墙体不是手写的。** `rawWalls(floor)` 的算法：

1. 取主体矩形 `[0,14.9] × [0,13.2]`；
2. 用该层所有房间矩形的边界坐标做网格分解（约 13×13 个单元格）；
3. 单元格中心不在任何房间内 → 该格是墙；
4. 沿 X 合并连续墙格，再沿 Z 合并同跨度墙格 → 得到墙体盒；
5. 按 `DOORS` / `WINDOWS` 把被洞口穿过的盒切成 左/右 + 洞下 + 洞上。

这个做法的性质决定了**不可能漏墙、不可能重叠、不可能侵入房间净空**，也就不可能出现"两个房间之间有缝可以穿过去"或"门被墙堵住"。
`tools/navcheck.mjs` 会额外断言"墙体不侵入任何房间净空"（当前 1F 83 个 / 2F 82 个墙盒，全部通过）。

同一批盒子直接作为碰撞体（`colliders()`），所以**视觉模型与物理模型物理上无法错位**。

## 2. 第一人称

| 参数 | 值 |
|---|---|
| 眼高 | 1.60 m |
| 圆柱半径 | 0.29 m |
| 身高（头部净空判定） | 1.70 m |
| 速度 | 1.5 m/s（Shift 2.6 m/s） |
| FOV | 70° |
| 重力 | 12 m/s² |
| 最大可跨台阶 / 吸附高度 | 0.30 m |

- **Pointer Lock**：在按钮点击的同一个用户手势内 `requestPointerLock()`（不等相机过渡结束，否则手势时效可能过期）。过渡期间 `player.frozen = true`，输入不生效。`ESC` 退出 → 自动回到轴测。
- **碰撞**：4 次迭代的圆柱-AABB 推出；只对与身体竖向区间重叠的盒生效，因此站在梯下储物体顶面（Y=1.55 的休息平台）时不会被自己脚下的盒子顶开。
- **支撑面** `supports(x,z)` 返回该点所有可站立高度（0 / 楼梯坡道 / 3.10），取 ≤ `y + 0.30` 的最高者。梯井内**只返回楼梯面**，杜绝"钻到楼梯底下"。
- **楼梯**：踏步是真实的 8+8 级踏面 + 休息平台；角色踩的是与之同坡度（**31.6°**）的隐形线性坡道，Y 连续变化。实测单帧最大 Y 变化 **0.0173 m**（60 fps 下），无瞬移、无抖动、无场景切换。
- **兜底**：若某帧算不出任何支撑面（理论上不该发生），角色退回上一帧位置而不是掉出世界。

## 3. 相机与 UI

- **默认轴测**：刷新后 `mode = 'axo'`。OrbitControls，俯角约 40°，`maxPolarAngle` 限制不会翻到地面以下。
- **二层抬升**：轴测时 `g2.position.y = 2.2`（X/Z 不变），屋面隐藏；第一人称时归零、屋面显示。抬升与相机同为约 1 s 缓动。
- **控制器互斥**：切换瞬间 `orbit.enabled = false`，由一台过渡相机接管画面做 `easeInOut` 插值（位置 lerp + 四元数 slerp + FOV），到位后才把控制权交给目标相机。另有兜底：若标签页被挂起导致过渡未完成，回到轴测时会自动恢复 Orbit。
- **户型图 Overlay**：优先加载 `public/floorplan.png`（你的原始图纸，等比显示、不重绘、不 OCR）；缺失时用 3D 模型同一份数据实时绘制概念图，并在界面上保留 **「概念参考方案 · 非施工图」** 说明。支持滚轮缩放、拖动、`⤢` 复位、`Esc` 关闭。
- **小地图（第一人称专用）**：右上角 188 px canvas，**北向上固定**（`-Z` 即图纸上方为北），显示当前楼层平面 + 角色圆点 + 70° 视锥 + 指北针 + `1F/2F 房间名`。
  底图由 `ROOMS` 与 `walls(floor)` 绘制，**与 3D 场景同源**，不会出现地图和实景对不上；两层各烘焙一次到离屏 canvas，每帧只 blit 一次再画一个箭头。
  楼层判定以 `y ≥ 1.55`（休息平台高度）为界，上楼走到平台时图面自动翻页；在梯井内房间名显示「楼梯」。
- **加载页**：真实进度（渲染器 → 几何 → 光照 → 相机 → UI）。任何异常都会被 `try/catch` 捕获并在加载页显示可读原因，不会永久黑屏。

## 4. 测试与证据

完整实时输出见 **`TESTS.md`**（共 **159 项断言，0 失败**）。

```bash
npm run check              # verify.mjs + navcheck.mjs
node tools/walktest.mjs    # 无头行走测试
```

| 测试 | 做什么 | 结果 |
|---|---|---|
| `verify.mjs` | 尺寸链闭合、面积、门洞、楼梯几何、上下层对位 | 全部通过 |
| `navcheck.mjs` | 以 0.10 m 网格 + 半径 0.29 m 做三维洪水填充（节点 = 位置 × 支撑面高度），从玄关出发 | **26 个空间全部可达**；24 个关键点按指定楼层高度逐一可达；墙体不侵入房间；28 552 个可达节点 |
| `walktest.mjs` | **驱动运行时同一个 `Player` 类**（真实碰撞 + 重力 + 坡道），以 60 fps 步进走完 96 个航路点的双向全程 | 全部到位；单帧最大 Y 变化 0.0173 m；单帧最大下落 0.0173 m；终点回到 Y=0.000 |

实测双向路线（`walktest.mjs`）：

> 玄关 → 过道 → 客厅 → 餐厅 → **厨房** → 走廊 → 公卫 → 洗衣房 → 储物间 → 玄关 →
> **楼梯（第一跑 → 平台 → 第二跑）** → 二层楼梯厅 → 上层休息厅 → 东走廊 → Linen → 北走廊 → 二层公卫 →
> 家庭起居室 → 次卧2 → 次卧3 → **阳台** → 主卧 → 衣帽间 → 主卫
> —— 再**原路反向**下楼，并补测多功能室、客卧/书房、露台，最后回到玄关。

**开发过程中被这三套测试抓到并修掉的真实缺陷**（都会导致你担心的那类 bug）：

1. 二层有两处 0.5 m 级的"无归属碎片区域"，墙体生成器会把它们变成无法通行的死角 → 调整走廊/公卫/Linen 边界消除。
2. 二层楼梯出口所在的墙带没有开洞 → 补 `d_stair_f2` 门洞，否则从二层根本走不到楼梯。
3. `d_linen` 门洞跨在两块不同朝向的墙盒之间，会残留一段 0.15 m 堵头 → 把门位从 z=8.95 移到 9.60。
4. 户型图 Overlay 在面板 `display:none` 时计算缩放，得到负的 scale，图纸飞出屏幕 → 改为显示后再 fit，并修正 `translate` 未随 scale 换算的问题。
5. Pointer Lock 在相机过渡结束（约 1 s 后）才请求，可能已超出用户手势时效 → 改为点击瞬间请求。

## 5. 已知限制（不作无证据承诺）

- **未测得帧率**。本机的无头预览浏览器不执行 `requestAnimationFrame`（实测 1.1 s 内 0 帧），因此**不对 60 fps 作任何承诺**。界面右下角有实时 fps / draw call 读数，请在你的桌面浏览器上自行读取；当前场景约 **5k 三角形 / 24 draw calls**（每种材质合并为一个 BufferGeometry）。
- 相机过渡、抬升动画、第一人称行走**只在无头环境之外可验证**；行走的正确性已由 `walktest.mjs` 用真实物理代码离线证明，但"手感"仍需你实机体验。
- 尚无家具、无 HDR、无后期（SSAO/Bloom）、材质为占位纯色 —— 全部属于阶段三。
- 门只有洞口没有门扇；窗为 15 mm 薄玻璃片。

## 6. 阶段三计划（待你放行）

1. 家具：程序化建模为主（带倒角、厚度、结构），按人体尺度与通行净宽布置；`verify.mjs` 增加"门开启扇区 0.9 m 见方内无家具"与"通行净宽 ≥0.9/0.8 m"检查，布置完立刻回归跑 `navcheck` + `walktest`。
2. 材质：MeshStandardMaterial / MeshPhysicalMaterial，木/石/微水泥/金属/布艺的 roughness·metalness·normal·AO，玻璃 transmission·IOR·thickness。
3. 光照：白天 HDR 环境 + 方向日光；夜晚少量关键灯 + emissive 灯具，色温 2700–3500 K。
4. 优化：InstancedMesh、纹理尺寸控制、GLB 压缩、按需加载；并在你指定的设备/分辨率上实测帧率后再写结论。
