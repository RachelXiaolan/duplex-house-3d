# 阶段三：精装与 ArchViz

风格：**Modern Warm Luxury / Warm Minimalism**（暖白涂料 + 橡木/胡桃木 + 石材台面 + 黄铜五金 + 米灰布艺）。

**建筑尺寸零改动**：`src/data/building.js` 在本阶段未动过一个数字，`tools/verify.mjs` 的尺寸链与楼梯校核结果与阶段二完全一致。

---

## 1. 家具

- **119 件**，覆盖两层全部房间 + 露台 + 阳台，数据在 [`src/data/furniture.js`](src/data/furniture.js)（纯数据，node 端测试脚本也读它）。
- **全部程序化建模**，无第三方 GLB。每件都由带倒角的圆角盒（`ExtrudeGeometry` + bevel）、圆柱、球组装，含腿脚、板厚、门板缝、抽屉、把手、坐垫/靠背垫、床垫/被/枕、台面出挑、挡水条、水龙头、玻璃淋浴屏等结构分件 —— **没有用纯方盒冒充成品家具**。
- 人体尺度按要求：主卧床 1.90×2.15、次卧床 1.60×2.10、客卧床 1.50×2.05；客厅沙发 3.00m、家庭厅沙发 2.20m；餐桌 2.40×1.05；厨房岛台高 **0.92m**、台面高 0.90m；吧椅 0.68m。
- `ry` 只取 0/90/180/270，保证碰撞 AABB 精确；`sw/sd` 可覆盖碰撞footprint（餐桌把座椅区一并计入）。
- 地毯、镜子、吊灯、摆件、餐椅、吧椅等 `solid:false` —— **小摆件不参与碰撞**，大件（沙发/床/柜/台/洁具）全部有碰撞盒。

### 家具布置后的回归（这是重点）

摆完家具**立刻**重跑可达性与行走测试，被抓到并修掉的真实问题：

| # | 问题 | 修法 |
|---|---|---|
| 1 | 餐桌横放后，把「餐厅↔厨房」门与「餐厅→露台」门同时挤成 0.60m 的夹缝（人体直径 0.58m，几乎卡死） | 餐桌转 90°，两侧留出 0.83 / 0.85m 通道 |
| 2 | 玄关长凳距「玄关↔走廊」门洞仅 0.16m，堵门 | 长凳移到玄关东南角 |
| 3 | Linen 储物北侧搁架距门洞 0.286m（< 0.29m 半径），刚好卡人 | 搁架西移并缩短 |
| 4 | 客厅单人椅挡住「客厅→客卧」的斜穿路径 | 单人椅移到 (2.20, 5.20) |
| 5 | 次卧3 床与衣柜之间只剩 0.55m，进不去 | 床东移 0.20m，通道 0.75m |
| 6 | `footprint()` 在 `ry=90` 时把 `sw/sd` 交换方向搞反，导致餐桌碰撞盒转错 90° | 修正数据 |
| 7 | `rbox()` 旋转后平移量写成 `h-b` 而非 `b`，**所有家具高度翻倍且悬空** —— 衣柜顶到 4.33m、冰箱到 3.82m | 修正为 `translate(0, b, 0)`，并新增 `furncheck.mjs` 永久防回归 |

### 新增第 3 套测试：`tools/furncheck.mjs`

在 node 里（stub 掉 canvas）真实构建每一件家具的几何，断言：
- 实际包围盒不超出声明 footprint（平面 ±0.16m）；
- 不低于该层完成面；
- 竖向不超出碰撞盒 +0.90m（床头板/龙头/吊杆等造型允许高于「挡人的体量」）；
- **顶面不得超过净高 2.90m**。

第 7 条 bug 正是被它一次性揪出来的（当时 119 件里 60+ 件报错）。

---

## 1.5 门窗构件

原先只有洞口，本阶段补齐（[`src/scene/openings.js`](src/scene/openings.js)）：

| 类型 | 数量 | 做法 |
|---|---|---|
| 门套（架线） | **28 处洞口全覆盖** | 双面 75mm 架线、出墙 18mm；外墙洞口只做室内侧 |
| 平开门扇 | **11 樘** | 橡木门扇 45mm 厚，**开启 90°** 立在合页侧，双面浅凹槽 + 黄铜执手 |
| 入户门 | 1 樘 | 与室内门一致**开启 90°**，1.20×2.40，黄铜执手在非合页侧 |
| 推拉门 | 3 樘（露台 ×2 / 阳台 ×1） | 一扇固定 + 一扇推开后叠放，停在洞口一半；黑色窄边框 + 玻璃 + 上下轨道 |
| 敞开洞口 | 13 处 | 客厅↔餐厅、走廊、楼梯口等，只做架线不装门扇 |
| 窗框 | **20 樘** | 黑色窄框 55mm + 宽窗加中梃 + 室内石材窗台 |

**门扇一律开启 90°**（而不是虚掩）有两个好处：门扇轴对齐 → 碰撞 AABB **精确**；而且它真实占用空间，能被测试校核。门扇位置直接取自 `geom/build.js` 的 `doorLeaf()`，**与碰撞体同源**。

装完门扇再跑测试，又抓到 2 个真实问题：

1. **次卧3 的门扇开进房间后，与床之间只剩 0.525m**，房间可站立面积掉到 5% —— 把合页从 `lo` 端换到 `hi` 端解决。
2. 行走测试中 3 段路线因门扇/推拉扇改变了净通道而卡住（次卧2 出入、露台门、阳台门）→ 航路点改走门扇让出的那一侧。

`furncheck.mjs` 新增第 2 项断言：**15 樘门扇/推拉扇开启后均不碰家具**（含入户门）（规范里"门扇开启不碰家具"这一条现在有自动校核了）。

## 1.6 楼梯扶手

沿两跑与休息平台连续布置（[`src/geom/build.js` `handrails()`](src/geom/build.js) + [`src/scene/house.js` `railGeoms()`](src/scene/house.js)）：

- **外侧一根全程连续**：第一跑西墙 → 平台西 → 平台北 → 平台东 → 第二跑东墙，转角球接过渡；
- **内侧两根**贴 0.25m 梯井分隔墙，各随梯段坡度；
- 规格：Ø0.044 m 黄铜圆管，**高出踏面 0.90 m**，距墙面 0.055 m；每 ~0.85 m 一个墙面托架（带底座圆盘）；两跑起步端各有一根落到踏面的立柱。
- **净宽影响**：梯段 1.10 m → 两侧扶手各占 0.0665 m → 净 **0.946 m**，远大于人体直径 0.58 m。
- 扶手同时进入**碰撞体**（`railColliders()`，XZ 精确、Y 取整段跨度保守估计），装完后 `navcheck` 与 `walktest` 全部重跑通过。

## 1.7 硬装（精装修）图层

阶段三原本只做了软装（家具 + 材质 + 灯光），硬装这层是空的。补齐在 [`src/scene/finishes.js`](src/scene/finishes.js)，全部由房间矩形与门窗表推导、**自动扣除门洞与门套**：

| 项目 | 做法 |
|---|---|
| **踢脚线** | 全部干区房间四周，高 100mm、出墙 18mm，白色烤漆；沿墙自动断开门洞 |
| **分区地面** | 主卫/公卫/洗衣房 → 大理石；厨房/玄关 → 微水泥；其余保留橡木地板 |
| **卫浴墙砖** | 4 个湿区房间四周贴到 **2.10m**，大理石纹 |
| **厨房防溅砖** | 东/北墙台面之上 0.90–1.50m |
| **电视背景墙** | 客厅西墙：橡木竖向格栅（24 根 @110mm）+ 胡桃木上下压条 |
| **主卧床头墙** | 北墙 3×2 分格软包，排在床头板顶（4.42m）之上 |
| **窗帘** | 14 樘低窗台窗：黑色窗杆 + 两侧亚麻落地帘（高窗台的卫生间小窗不做） |
| **筒灯** | 面积 >5.5m² 的房间按 ~2.2m 网格布置，Ø144mm |
| **灯带（回光灯槽）** | 客厅 / 主卧 / 家庭起居室，沿墙内缩 350mm 一圈 |
| **挂画** | 4 幅，胡桃木框 + 画布 |

筒灯与灯带复用灯罩材质，**夜间自动发光**（跟随昼夜开关），不新增材质也不新增光源。

**装完后暴露并修掉的 2 个真实问题：**

1. **客厅电视背景墙正好糊住西窗** —— 根源是家具布置阶段就把电视摆在了窗前（实际会眩光）。把 `w_living_w` 由 z=3.20 南移到 **z=5.70**，背景墙收到 z 1.90–4.65。这是本阶段唯一一次改 `building.js`，改的是窗户位置、不是尺寸，`verify.mjs` 尺寸链校核不受影响。
2. 主卧软包与床头板在 z 向穿插 —— 软包上移到床头板顶之上（`F2+1.45m` 起）。

**代价**：draw call 60–85 → **86–97**，三角形 ~120k → **141k**。硬装全部按材质合并，且**不参与碰撞**，因此四套测试 193 项断言重跑仍全部通过。

## 2. 材质

[`src/scene/materials.js`](src/scene/materials.js) —— **全部纹理由 canvas 程序化生成**，项目仍然是零第三方贴图 / 零 HDR，无许可风险、无加载失败可能。

- `woodCanvas()`：正弦年轮 + 随机纹理条，做出橡木 / 胡桃木两套色。
- `stoneCanvas()`：随机游走折线画大理石脉络 + 噪声叠加。
- `noiseCanvas()` + `normalFrom()`（Sobel）：由高度图算法线图，用于涂料、微水泥、织物（织物法线强度 3.4，做出布纹颗粒）。
- 参数：涂料 rough 0.96 / 木材 0.50 / 石材 0.28 metal 0.02 / 布艺 0.96 / 黄铜 metal 1.0 rough 0.28 / 陶瓷 rough 0.12。
- 玻璃用 `MeshPhysicalMaterial`：`transmission 0.94`、`ior 1.5`、`thickness 0.02`、`roughness 0.03`。
- 地毯用**顶点色 + 合批**：每块地毯颜色不同但仍然只占 1 个 draw call。

---

## 3. 光照与后期

- **环境光照**：`RoomEnvironment` 经 `PMREMGenerator` 烘成 IBL（`scene.environment`）—— 程序化室内环境，不依赖任何外部 HDR 文件。
- **白天**：IBL 强度 0.62 + 半球光 0.32 + 方向日光 1.9（暖白 `0xffe6c0`，2048² PCF 软阴影）+ 环境光 0.04，曝光 0.95。
- **夜晚**：IBL 降到 0.09、日光 0.10（冷蓝月光）、14 盏关键点光按灯具位置点亮（色温 2700–3500K，`0xffbe82`–`0xffd9b0`），灯罩材质切到 `emissiveIntensity 2.4`，曝光 1.05。点光挂在楼层 Group 上，轴测抬升时随层移动。
- **后期**：`EffectComposer` = RenderPass → UnrealBloom（白天 strength **0.14**、夜晚 0.34，threshold 0.92 —— 刻意压低，避免过度泛光）→ OutputPass（负责 ACESFilmic 与色彩空间）。构建失败时 `try/catch` 自动退回直接渲染。
- **GTAO/SSAO 未启用**：本机无头浏览器不执行 `requestAnimationFrame`，我无法实测它的开销与在大量透射玻璃下的伪影。宁可不加，也不做没有证据的承诺。需要的话可以后续开一版给你实测对比。

---

## 4. 性能优化（针对实测 20fps）

你反馈实测 20fps（≈50ms/帧）。几何量并不大（12–16 万可见三角形、60–100 draw call），50ms 基本只能是**逐帧的重复工作与填充率**，逐条排查后做了 4 项修改：

| # | 问题 | 修法 | 效果 |
|---|---|---|---|
| 1 | 玻璃用 `MeshPhysicalMaterial` + `transmission` —— three 为此**每帧额外把整个场景再渲染一遍**到透射 RT | 改用带 `envMap` 反射的半透明 `MeshStandardMaterial`（opacity 0.13） | 直接省掉一整遍场景绘制 |
| 2 | `shadowMap.autoUpdate` 默认开，2048² 阴影贴图**每帧重画**，而这栋房子是静态的 | `autoUpdate=false`，仅在楼层抬升 / 昼夜切换 / 画质切换时置 `needsUpdate` | 轴测全景 **3.6ms → 2.0ms（−44%）** |
| 3 | 14 盏夜间点光白天只是 `intensity=0`，**仍留在场景里**，每个像素照样跑 14 次光照计算 | 白天把点光整个 `removeFromParent()`，夜间才挂回 | 白天着色器光照循环从 17 盏降到 3 盏 |
| 4 | 固定 `devicePixelRatio`，Retina 上是 4× 片元 | **自适应画质 4 档**（DPR / Bloom / 阴影），从「中高」起步，fps<40 连续 2 窗降档、fps>56 连续 6 窗回升 | 低配自动兜底，高配自动回到最高画质 |

**本机实测**（WebKit WebGL，`gl.finish()` 强制同步后取 15 帧均值）：

```
轴测全景 · 静态阴影        2.0 ms/帧
轴测全景 · 每帧重画阴影    3.6 ms/帧      ← 修改前的行为
第一人称 · 客厅白天        0.5 ms/帧
第一人称 · 客厅夜晚        1.0 ms/帧
draw call                  150 → 60~85
```

⚠️ **这些数字来自我这台机器的浏览器，不能直接套到你的机器上**（分辨率、DPR、GPU 都不同）。请你重新跑一遍，读右下角的 `fps · draw calls · tris · 画质 X`，把数字和你的设备/分辨率告诉我。如果还是不到 40fps，下一步可以做的杠杆是：夜间只点亮当前楼层的灯（少一半点光）、把家具按房间拆成多个 Mesh 以便视锥剔除、阴影贴图降到 1024²。

## 5. 优化（几何与批次）

- 建筑与家具**按材质合并 BufferGeometry**：整栋 1F/2F 各约 10 个 Mesh。
- 几何统一转非索引后再合并（`ExtrudeGeometry` 非索引 / `Cylinder` 索引，混合会让 `mergeGeometries` 返回 null —— 这是开发中遇到的另一个真实报错）。
- 纹理 256–512px，`anisotropy 8`；共享贴图各只生成一次。
- 点光不投影（只有太阳投影），阴影贴图 2048²。
- 当前场景：**约 40 万三角形**；HUD 实时显示 draw call（累计了阴影 pass 与 bloom 多 pass，故读数在 130–150 之间）。

---

## 6. 已知限制（不作无证据承诺）

- **我仍然无法在本机测出真实 fps**（无头浏览器不跑 `requestAnimationFrame`）。§4 的 ms/帧 是用 `gl.finish()` 强制同步逐帧计时得到的，是真实测量，但只代表我这台机器。**本项目不对 60fps 作任何承诺**，请以你机器上的实测为准。
- 门扇是静态的（全部固定为开启姿态），不能交互开合；玻璃未做分格贴花。
- 玻璃已去掉真实折射（transmission），改为反射 + 半透明近似 —— 这是为帧率做的取舍，如果你更看重玻璃质感、机器也跑得动，可以改回去。
- 家具是程序化的「形似」而非产品级还原；软包缝线、织物褶皱、书籍/餐具等小摆件未做。
- **筒灯与灯带只是自发光材质，不真的照亮房间**（夜间照明仍由 14 盏关键点光承担）。做成真实光源需要再加 40+ 盏灯，帧率代价太大 —— 这是明确的取舍。
- 硬装未做：吊顶跌级造型、石膏线、瓷砖填缝、地板拼缝、墙纸/护墙板（除电视墙与床头墙外）。
- 第三方资源：**依旧为零**。运行时只依赖 three.js（MIT），构建依赖 Vite（MIT）。
